{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "npm run build",
        "outputDirectory": "dist/portfolio-website"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "destination": "/index.html" }
  ]
}